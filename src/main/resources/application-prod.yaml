spring:
  datasource:
    url: jdbc:postgresql://ikramshoteldb2025.postgres.database.azure.com:5432/hoteldb?sslmode=require
    username: postgres@ikramshoteldb2025
    password: Febrex123
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      auto-commit: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

hotel:
  app:
    jwtSecret: dev-secret-123456
    jwtExpirationMs: 3600000
