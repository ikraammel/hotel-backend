spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}  # prod par défaut
  logging:
    level:
      org.springframework: DEBUG
hotel:
  app:
    jwtSecret: dev-secret-123456
    jwtExpirationMs: 3600000